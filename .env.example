# Copy to .env and adjust. .env is gitignored; this template is not.
# Read by BOTH Docker Compose (compose.yaml) and Rails (via dotenv-rails).

# Local Postgres runs in Docker. DB_PORT is the host-side port of the mapping.
DB_HOST=localhost
DB_PORT=5434
DB_USER=postgres
DB_PASSWORD=change_me
